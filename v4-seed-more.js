(() => {
  const M=window.MM,$=M.$,$$=M.$$,n=M.num,fmt=M.fmt,safe=M.safe;
  M.renderSeed=()=>{const rows=M.state.seed.rows,actual=rows.reduce((a,b)=>a+n(b.actual),0),goal=n(M.state.seed.longGoal),pct=goal?Math.min(100,actual/goal*100):0;$('#screen-seed').innerHTML=`${M.banner()}<p class="eyebrow">SEED</p><h1 class="page-title">자본 만들기</h1><div class="grid two-col"><article class="card dark"><p class="eyebrow">LONG TERM</p><div class="amount">${fmt(actual,M.state.seed.targetCurrency)}</div><div class="muted">목표 ${fmt(goal,M.state.seed.targetCurrency)} · 계획시작 ${M.state.seed.planStart}</div><div class="progress" style="margin-top:12px"><i style="width:${pct}%"></i></div></article><article class="card"><div class="field"><label>계획 시작월</label><input class="input" type="month" value="${M.state.seed.planStart}" data-seed-start></div><div class="field" style="margin-top:8px"><label>장기 목표</label><input class="input num" type="number" value="${goal}" data-seed-goal></div></article></div><div class="section-head"><div><h2>월 목표 / 실제</h2><p>부업이 먼저 터지면 시작월을 앞당겨도 됨.</p></div></div><div class="seed-months">${rows.map((r,i)=>`<div class="seed-month ${r.month<M.state.seed.planStart?'inactive':''}"><div class="month-name">${r.month}</div><div class="field"><label>목표</label><input class="input num" type="number" value="${n(r.target)}" data-seed-target="${i}"></div><div class="field"><label>실제</label><input class="input num" type="number" value="${n(r.actual)}" data-seed-actual="${i}"></div></div>`).join('')}</div>`};
  M.renderMore=()=>{$('#screen-more').innerHTML=`${M.banner()}<p class="eyebrow">MORE</p><h1 class="page-title">설정 · 기준값</h1><div class="grid two-col"><article class="card"><div class="card-title"><h3>데이터 시작점</h3></div>${['JPY','KRW','USD'].map(c=>`<div class="line"><span>${c} 시작잔고</span><strong>${fmt(M.state.settings.startBalances[c],c)}</strong></div>`).join('')}<div class="btn-row" style="margin-top:10px"><button class="btn ghost" data-edit-start>시작점 편집</button></div></article><article class="card"><div class="card-title"><h3>환율</h3><button class="btn ghost small" data-add-fx>＋ 등록</button></div>${M.state.fx.length?M.state.fx.map(x=>`<div class="line"><span>${x.month}</span><strong>¥100=₩${n(x.krwPer100Jpy)||'—'} · $1=¥${n(x.jpyPerUsd)||'—'}</strong></div>`).join(''):'<div class="empty">환율 미등록</div>'}</article></div><div class="section-head"><div><h2>카드 계정</h2><p>이름만 관리. 월 카드값은 큰 지출에서 입력.</p></div><button class="btn ghost small" data-add-card>＋ 카드</button></div><article class="card">${M.state.cardAccounts.map((x,i)=>`<div class="settings-row"><input class="input" value="${safe(x.name)}" data-card-name="${i}"><select class="select" data-card-cur="${i}"><option ${x.currency==='JPY'?'selected':''}>JPY</option><option ${x.currency==='KRW'?'selected':''}>KRW</option><option ${x.currency==='USD'?'selected':''}>USD</option></select><button class="delete-x" data-del-card="${i}">×</button></div>`).join('')}<button class="btn lime block" style="margin-top:10px" data-save-cards>카드 설정 저장</button></article><div class="section-head"><div><h2>고정비 템플릿</h2><p>월세·통신·보험 같은 반복 큰돈. 새 월에 한 번에 적용.</p></div><button class="btn ghost small" data-add-fixed>＋ 항목</button></div><article class="card">${M.state.fixedTemplates.map((x,i)=>`<div class="fixed-edit-row"><input class="input" value="${safe(x.name)}" data-fix-name="${i}"><select class="select" data-fix-cur="${i}"><option ${x.currency==='JPY'?'selected':''}>JPY</option><option ${x.currency==='KRW'?'selected':''}>KRW</option><option ${x.currency==='USD'?'selected':''}>USD</option></select><input class="input num" type="number" value="${n(x.amount)}" data-fix-amt="${i}"><button class="delete-x" data-del-fixed="${i}">×</button></div>`).join('')}<button class="btn lime block" style="margin-top:10px" data-save-fixed>고정비 템플릿 저장</button></article><div class="section-head"><div><h2>정산</h2><p>리보와 한국 지원금은 완료되면 메인에서 사라짐.</p></div></div><div class="obligation-grid">${M.state.settlements.map((x,i)=>`<article class="obligation"><div class="head"><h3>${safe(x.title)}</h3><strong>${fmt(x.current,x.currency)}</strong></div><div class="field-grid"><div class="field"><label>현재 잔액</label><input class="input num" type="number" value="${n(x.current)}" data-set-cur="${i}"></div><div class="field"><label>이번 계획</label><input class="input num" type="number" value="${n(x.planned)}" data-set-plan="${i}"></div></div></article>`).join('')}</div><button class="btn lime block" style="margin-top:10px" data-save-settlement>정산 저장</button>`};
  M.addFx=()=>{M.openSheet(`<h2 class="sheet-title">환율 등록</h2><div class="field"><label>적용월</label><input id="fxMonth" class="input" type="month" value="${M.selectedMonth}"></div><div class="field-grid" style="margin-top:8px"><div class="field"><label>¥100 = ₩</label><input id="fxKrw" class="input num" type="number"></div><div class="field"><label>$1 = ¥</label><input id="fxUsd" class="input num" type="number"></div></div><button id="fxSave" class="btn lime block" style="margin-top:10px">저장</button>`);$('#fxSave').onclick=()=>{const m=$('#fxMonth').value;M.state.fx=M.state.fx.filter(x=>x.month!==m);M.state.fx.push({id:`fx-${m}`,month:m,krwPer100Jpy:n($('#fxKrw').value),jpyPerUsd:n($('#fxUsd').value),note:''});M.save();M.closeSheet();M.renderAll();M.toast('환율 저장')}};
})();